description = "打开 Playwright UI 交互模式"
prompt = """
# PLAYWRIGHT UI DIRECTIVE

The user wants to open Playwright's interactive UI mode.

## Action
- Run `npx playwright test --ui`.
- Inform the user that the UI window will open and they need to close it to resume the chat session (or run it in background if preferred).
"""

